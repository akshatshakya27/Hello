#include<stdio.h>
int main(){
    int n,i,m;
    printf("Enter the number:\n");
    scanf("%d",&n);
    i=1;
    while(i<=10){
        m=n*i;
        printf("%d x %d = %d\n",n,i,m);
        i++;
    }
    return 0;
}
